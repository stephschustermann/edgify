import React from "react";

const game_over = props => (
    <div>
        <title> Start New Game </title>
        <button> Click here to start!</button>
    </div>
);
// TODO add here action to init a new game
export default game_over